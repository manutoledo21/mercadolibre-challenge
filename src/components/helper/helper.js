export const formatterCurrency = new Intl.NumberFormat('es-AR', {
  style: 'currency',
  currency: 'ARS',
});

// // Create our number formatter.
// console.log(formatter.format(2500)); /* $2,500.00 */
